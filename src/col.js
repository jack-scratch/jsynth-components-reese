const js = "#f0db4f";

const bg = "#333";
const fg = "grey";

const inert = "#111";

const light = {
	"inert": "rgba(240, 219, 79, 0.8)",
	"active": "rgba(0, 0, 0, 0.2)"
};

export {
	js,

	bg,
	fg,

	inert,

	light
};
